# Output Style

Public export note: detailed style preferences are private overlay material.

The shared default is:

- keep responses concise and evidence-grounded
- preserve exact commands, file paths, errors, risks, and verification results
- do not use compression style as permission to skip facts, tests, or caveats

Deployment-specific tone rules can live in a private `rules/` overlay.
